## Plan: Top Bar Prototype Alignment

To match the requested prototype deltas, apply these exact changes:

- Make header thin and single-line: title + menu + right counter all in one row.
- Move group names to the left of each icon set (inline group label, not above).
- Move link counter out of Links group to the far-right header badge.
- Keep icon-only buttons and tooltip descriptions.
